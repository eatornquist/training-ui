// import { communityRowsData, communityColumnsData } from '../data'
import React, { useState } from 'react'

import { DataGrid } from '@mui/x-data-grid'

export default function CommunityList() {
  return (
    <>
      {/* <DataGrid
        // className="table"
        rows={communityRowsData}
        columns={communityColumnsData}
        initialState={{
          pagination: {
            paginationModel: { page: 0, pageSize: 5 },
          },
        }}
        pageSizeOptions={[5, 10]}
        checkboxSelection
      /> */}
    </>
  )
}
